#!/bin/zsh

# Update
sudo pacman -Syu
abs

# Clean up
sudo pacman -Rsc $(pacman -Qtdq)
sudo localepurge
sudo pacman -Scc

# General maintenence
sudo pacman-optimize
sudo mandb
sudo ldconfig -v
sudo updatedb
sudo sync
