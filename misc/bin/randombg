#!/usr/bin/env zsh

WALLPAPERDIR=${1:-/mnt/data/wallpaper/landscape}

if [[ ! -d $WALLPAPERDIR ]]; then
  echo "Missing wallpaper directory."
  exit
fi

feh --bg-tile "$WALLPAPERDIR/`ls $WALLPAPERDIR | shuf -n1`"
