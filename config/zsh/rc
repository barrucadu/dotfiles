## -*- shell-script -*-

. ~/.zsh/profile
. ~/.zsh/functions
. ~/.zsh/aliases

# Start X
if [[ -z "$DISPLAY" ]] && [[ $(tty) == /dev/tty2 ]] && [[ "`runlevel`" == *5* ]]; then
    while true; do
        echo "Starting X11"
        xinit &>/dev/null
        source ~/.zsh/profile
    done
fi
